<div class="row">
  <div class="span3"><h1>Recipe history</h1></div>
</div>
<div class="row-fluid">
	<% recipe  = Recipe.find(params[:id]) %>
	<!-- TODO: Find the complete history -->
	<% while recipe.parent != nil %>
		<% p = Recipe.find(recipe.parent) %>
		<%= render "recipes/thumbnail_block", :recipe => p %>
		<% recipe = p %>
    <% end %>
  </ul>
</div>